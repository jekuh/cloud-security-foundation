# Azure Secure Foundation (scaffold)

Independent Azure folder mirroring AWS structure.
Implement later:
- `bootstrap/state`: Storage Account + container for Terraform state
- `bootstrap/oidc`: Entra ID federated credentials for GitHub Actions OIDC
- `envs/dev` and `envs/prod`
- `modules` (identity/compute/storage/monitoring)
