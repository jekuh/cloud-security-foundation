# GCP Secure Foundation (scaffold)

Independent GCP folder mirroring AWS structure.
Implement later:
- `bootstrap/state`: GCS bucket for Terraform state
- `bootstrap/oidc`: Workload Identity Federation for GitHub Actions
- `envs/dev` and `envs/prod`
- `modules` (identity/compute/storage/monitoring)
